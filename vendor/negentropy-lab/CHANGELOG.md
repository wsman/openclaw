# Changelog

## 2026-03-04

- Added `phase14:e2e` script to run OFF/SHADOW/ENFORCE decision-mode integration checks with HTTP/WS probes.
- Added `phase14:perf` script to produce latency, WebSocket success-rate, and throughput baseline artifacts.
- Updated deployment and README docs with Phase 14 validation commands.
